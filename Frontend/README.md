# Vaultly Frontend

The Lovable editor runs the frontend from the repository root (`src/`, `package.json`, and `vite.config.ts`). This folder is the named frontend boundary for the split-repository architecture; move the root frontend files into this folder when exporting the browser app to its own repository.